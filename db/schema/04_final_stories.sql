-- DROP TABLE IF EXISTS final_stories CASCADE;

-- CREATE TABLE final_stories (
--   id SERIAL PRIMARY KEY NOT NULL,
--   name VARCHAR(255) NOT NULL,
--   content TEXT,
--   user_id INTEGER REFERENCES users(id) ON DELETE CASCADE
-- )
